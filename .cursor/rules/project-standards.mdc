---
description: Getting Over It 项目结构与架构规则
globs: 
alwaysApply: true
---

# Project Structure

## C#（BepInEx 插件，Unity）

- `src/GameRuntime_v2/Core/` — 插件入口（`GameRuntimeManager`）、配置（`RuntimeConfig`）、接口定义、事件总线、工具类
- `src/GameRuntime_v2/PlayerControl/` — 状态读取（`PlayerStateService`）、输入注入（`PlayerInputService`）、多 Agent 复制（`PlayerDuplicateManager`）
- `src/GameRuntime_v2/ColliderCollection/` — 碰撞体收集与导出
- `src/GameRuntime_v2/Communication/` — TCP 帧级步进服务端（`IStepServer` / `TcpStepServer`，默认端口 9000）
- `src/GameRuntime_v2/Testing/` — 调试工具（`IDebugTool` / `PlayerDebugTool`）

## Python

- `src/env/` — TCP 客户端 RL 环境（`GoiEnv`）
- `src/start/` — 游戏启动（`GameLauncher`）与模式控制（`GameModeController`）
- `src/config/project.json` — 集中配置：游戏可执行路径、TCP 端口
- `src/tests/` — 测试脚本
- `src/Data/` — 数据集与运行结果

## 通信架构

Python 作为 TCP 客户端，C# 作为服务端（`TcpStepServer`）。每个 RL step 对应一次阻塞请求-响应，保证帧级别同步。端口从 `project.json` 中读取，禁止硬编码。

## 通用规范

- 文件移动时同步更新 `.csproj`
- 路径拼接使用 `Path.Combine`
- 每个功能目录保持单一职责；新功能放入对应目录，不新建顶层目录
