# Communication - UDP 通信服务

UDP 双向通信（接收动作、发送状态）。

## 📄 文件列表（待实现）

| 文件 | 说明 |
|------|------|
| **UDPReceiveService.cs** | UDP 接收服务（从 Python 接收动作，端口 12345） |
| **UDPSendService.cs** | UDP 发送服务（向 Python 发送状态，端口 12346） |
| **UDPProtocol.cs** | UDP 协议实现（实现 IProtocol 接口） |

